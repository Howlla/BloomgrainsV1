import wretch from 'wretch';

import { BASE_URL } from '../constants';

// export const loginApi = wretch(`${BASE_URL}/login`);
export const loginApi=wretch('')
export const baseApi = wretch(BASE_URL);
